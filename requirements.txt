# check_proposals.py and check_votemarket.py use stdlib only (urllib, json, os, time, datetime).
# check_votemarket_frax.py needs web3 to read Frax's Votemarket bounty contract directly
# on-chain (that market has no public REST API the way Curve/f(x)/Yield Basis do).
web3>=6.0.0,<8.0.0
